<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullscreenTheme">
        <activity
            android:name=".MainMenuScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_main_menu_screen"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".gamecode.GameActivity"
            android:label="GameActivity"
            android:theme="@style/FullscreenTheme"/>

        <activity
            android:name=".gamecode.Asteroids.AsteroidsActivity"
            android:label="AsteroidsActivity"
            android:theme="@style/FullscreenTheme"/>

        <activity
            android:name=".gamecode.Snake.SnakeActivity"
            android:label="SnakeActivity"
            android:theme="@style/FullscreenTheme"/>
    </application>

</manifest>